# Base URL of the OCR backend. Must be reachable from the device/emulator.
# Use an https:// URL in production so uploads and responses are encrypted.
# In development you may point this at a local server, e.g. http://<your-ip>:8000
EXPO_PUBLIC_API_URL=https://your-backend.example.com
